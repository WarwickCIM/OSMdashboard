% Generated by roxygen2
% Please edit documentation in R/database.R
\name{connect_osm_db}
\alias{connect_osm_db}
\title{Connect to the OSM DuckDB database}
\usage{
connect_osm_db()
}
\value{
A DBI connection object (\code{DBI::DBIConnection}) to a DuckDB database.
You are responsible for closing it with \code{DBI::dbDisconnect(con)}.
}
\description{
Opens a DBI connection to the OSM Changesets DuckDB file.
It first checks the environment variable \code{DATABASE_URL} for a URL of the form
\verb{duckdb:///path/to/osm_changesets.duckdb}. If not set, it falls back to
the environment variable \code{DUCKDB_PATH} (default:
\code{"database/osm_changesets.duckdb"}).
}
\examples{
\dontrun{
  con <- connect_osm_db()
  # ... use the connection ...
  DBI::dbDisconnect(con)
}
}
